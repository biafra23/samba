plugins {
    id 'java'
    id 'maven-publish'
}

publishing {
    publications {
        samba(MavenPublication) {
            from components.java
            artifactId = project.name
        }
    }
    repositories {
        mavenLocal()
    }
}
